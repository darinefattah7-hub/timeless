<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
 
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;500&display=swap" rel="stylesheet">
 
    <link rel="stylesheet" href="/homepage.css">
    <link rel="stylesheet" href="/subpages.css">
</head>
 
<body>
 
<% if (role === 'admin') { %>
    <%- include('includes/adminDashboard.ejs') %>
<% } else { %>
    <%- include('includes/header.ejs') %>
<% } %>
 
<div class="products-container united-card">
 
    <div class="products-header" style="text-align:center;">
        <h1><%= pageTitle %></h1>
 
        <% if (category) { %>
            <p style="color:#777; font-size:1.1rem;">Showing all <b><%= category %></b> products</p>
            <a href="/allProducts" style="color:#333; text-decoration:none;">← View all products</a>
        <% } %>
    </div>
 
    <% if (products && products.length > 0) { %>
 
        <div class="products-grid">
 
            <% products.forEach(product => { %>
                <div class="product-card united-card">
 
                    <img src="/images/<%= product.imageGold || product.imageSilver || product.imageRoseGold %>"
                         alt="<%= product.name %>"
                         class="product-image">
 
                    <div class="product-info">
 
                       
 
                        <div class="product-name"><%= product.name %></div>
 
                        <p class="product-description"><%= product.description %></p>
 
                        <div class="product-details">
                            <div>
                                <div class="product-price">$<%= parseFloat(product.price).toFixed(2) %></div>
                                <div style="font-size:.85rem; color:#777;"><%= product.material %></div>
                            </div>
 <% if (role === 'admin') { %>
                            <div class="product-quantity <%= product.quantity < 10 ? 'low-stock' : '' %>">
                                Stock: <%= product.quantity %>
                            </div><% } %>
                        </div>
 
                        <button class="btn-united"
                                onclick="window.location.href='/products/<%= product.ID %>'">
                            View Details
                        </button>
 
                        <% if (role === 'admin') { %>
                        <div class="admin-actions" style="margin-top:15px;">
                           
                            <a href="/admin/editProduct/<%= product.ID %>">
                                <button class="btn-united-outline" >Edit</button>
                            </a>
 
                            <form action="/admin/delete-product/<%= product.ID %>" method="POST">
                                <button class="btn-united-outline"
                                        >
                                    Delete
                                </button>
                            </form>
 
                        </div>
                        <% } %>
 
                    </div>
                </div>
            <% }) %>
        </div>
 
    <% } else { %>
 
        <div class="no-products" style="text-align:center; padding:60px;">
            <p>No products found.</p>
 
            <% if (role === 'admin') { %>
                <button class="btn-united"
        onclick="window.location.href='/admin/add/<%= category %>'">
    Add New Product
</button>
 
            <% } %>
        </div>
 
    <% } %>
 
</div>
 
<%- include('includes/footer.ejs') %>
</body>
</html>
 
 