<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= pageTitle %></title>

  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"/>
  <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;1,400&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="/homepage.css"/>
  <link rel="stylesheet" href="/responsive.css"/>
  <link rel="stylesheet" href="/subpages.css"/>
</head>
<body>
 <% if (role === 'admin') { %>
  <%- include('includes/adminDashboard.ejs') %>
<% } else { %>
  <%- include('includes/header.ejs') %>
<% } %>

<div class="united-form">
  <h2>Customer Service Feedback</h2>

  <% if (isAuthenticated) { %>

  <form action="/user/addfeedbackCustomerService" method="post">

    <div class="form-group">
      <label>Your service experience</label>
      <textarea name="service-experience" rows="3"  placeholder="Your Feedback" required></textarea>
    </div>

    <div class="form-group">
      <label>Did you experience checkout difficulty?</label>
      <div class="radio-group">
        <label><input type="radio" name="service-assistance" value="Yes" required> Yes</label>
        <label><input type="radio" name="service-assistance" value="No" required> No</label>
      </div>
    </div>

    <div class="form-group">
      <label>Was the help section useful?</label>
      <div class="radio-group">
        <label><input type="radio" name="service-help" value="Yes" required> Yes</label>
        <label><input type="radio" name="service-help" value="No" required> No</label>
      </div>
    </div>

    <button type="submit">Submit Feedback</button>
  </form>

  <% } else { %>
    <p style="text-align:center;">Please <a href="/auth/login"><b>log in</b></a> to leave feedback.</p>
  <% } %>
</div>

<%- include('includes/footer.ejs') %>


</body>
</html>
